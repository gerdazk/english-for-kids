const HtmlHelper = require('./HtmlHelper');
const LocalStorage = require('./LocalStorage');
const SoundPlayer = require('./SoundPlayer');
const Words = require('./Words');

module.exports = {
  HtmlHelper, LocalStorage, SoundPlayer, Words,
};
